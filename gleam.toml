name = "glammy"
version = "0.1.0"
description = "A Telegram Bot Framework for Gleam — a port of grammY (https://github.com/grammyjs/grammY)."
licences = ["MIT"]
target = "erlang"
repository = { type = "github", user = "castletaste", repo = "glammy" }
links = [
  { title = "GitHub", href = "https://github.com/castletaste/glammy" },
  { title = "grammY", href = "https://grammy.dev" },
]

# Dependencies are pinned and all from the official `gleam-lang` GitHub org on
# hex.pm. Do not add any third-party deps without explicit vetting — bot tokens
# are sensitive credentials.
[dependencies]
gleam_stdlib = ">= 1.0.0 and < 2.0.0"
gleam_erlang = ">= 1.3.0 and < 2.0.0"
gleam_json = ">= 3.0.0 and < 4.0.0"
gleam_http = ">= 4.0.0 and < 5.0.0"
gleam_httpc = ">= 5.0.0 and < 6.0.0"

[dev_dependencies]
gleeunit = ">= 1.2.0 and < 2.0.0"
